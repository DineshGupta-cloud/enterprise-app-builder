{
  "name": "enterprise-app-builder-generator",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Deterministic enterprise application generator for Enterprise App Builder",
  "scripts": {
    "generate": "node src/cli.js",
    "validate": "node src/cli.js --validate",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
