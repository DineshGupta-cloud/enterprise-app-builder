# Relationship generation

Relationship-aware CRUD is the next runtime layer. The generator will consume ProjectSpec relationships and emit lookup APIs, foreign-key mappings, many-to-many join migrations, and relationship-aware React forms.
